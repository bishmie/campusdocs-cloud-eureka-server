
server:
  port: 8761

spring:
  application:
    name: eureka-server

  config:
    import: "optional:configserver:http://config.platform:8888"


  cloud:
    config:
      uri: http://config.platform:8888

